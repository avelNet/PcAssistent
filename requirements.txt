# HTTP / Async
aiohttp>=3.9

# Файловые события (inotify)
watchdog>=4.0

# Системный мониторинг
psutil>=5.9

# Конфиг
pyyaml>=6.0

# Офлайн TTS (Silero v4) — опционально, fallback если cloud TTS недоступен
# Раскомментируй если хочешь использовать engine: "silero" в config.yaml
# torch>=2.0        # pip install torch --index-url https://download.pytorch.org/whl/cpu
# scipy>=1.11

# Системные утилиты (устанавливаются через apt, не pip):
#   alsa-utils   — aplay для воспроизведения WAV (cloud TTS: salute/yandex)
#   libnotify-bin — notify-send ≥0.8 для кликабельных уведомлений
#   wl-clipboard  — wl-paste для буфера обмена на Wayland
#   xdotool       — управление окнами (опционально, X11/XWayland)
